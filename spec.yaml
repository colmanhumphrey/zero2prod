name: zero2prod
region: NYC1
services:
  - name: zero2prod
    dockerfile_path: Dockerfile
    source_dir: src
    github:
      branch: main
      deploy_on_push: true
      repo: ColmanHumphrey/zero2prod
    health_check:
      http_path: /health_check
    http_port: 8000
    instance_count: 1
    instance_size_slug: basic-xxs
    routes:
      - path: /
